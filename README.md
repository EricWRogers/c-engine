# c-engine

# ubuntu
sudo apt install gcc gdb git clang ccache cmake libharfbuzz-dev libopusfile-dev libflac-dev libxmp-dev libfluidsynth-dev libwavpack-dev libbz2-dev libglew-dev podman patchelf lld fuse